apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-operator-crd.fullname" . }}
  labels:
    {{- include "grafana-operator-crd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-operator-crd.selectorLabels" . | nindent 4 }}
