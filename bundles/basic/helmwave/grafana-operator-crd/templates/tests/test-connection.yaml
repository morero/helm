apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-operator-crd.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-operator-crd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-operator-crd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
